version: '2'
services:
  web:
    # env_file: .env
    build:
      context: . 
    ports:
      - "4000:4000"
    volumes:
      - .:/usr/src/app
#      - /usr/src/app/node_modules
    command: mix phoenix.server